# Local development defaults
VITE_BASE_PATH=/
API_PROXY_TARGET=http://127.0.0.1:3001
PI_SSH_TARGET=user@todo-pi.local

# Optional sample projects/tasks/inventory for a disposable development database.
# Ignored by production builds; existing resources are never replaced.
VITE_DEMO_DATA=false
