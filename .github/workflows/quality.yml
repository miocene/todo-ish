name: Quality checks

on:
  pull_request:
  workflow_call:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  checks:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    strategy:
      fail-fast: false
      matrix:
        node: [22.22.1, 24.19.0]
    services:
      postgres:
        image: postgres:18.6-bookworm
        env:
          POSTGRES_PASSWORD: postgres
          POSTGRES_DB: postgres
        ports:
          - 5432:5432
        options: >-
          --health-cmd "pg_isready -U postgres"
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
    steps:
      - name: Check out repository
        uses: actions/checkout@v7

      - name: Set up Node.js
        uses: actions/setup-node@v7
        with:
          node-version: ${{ matrix.node }}
          cache: yarn
          cache-dependency-path: |
            yarn.lock
            backend/api/yarn.lock

      - name: Enable Yarn
        run: corepack enable

      - name: Set up Python
        uses: actions/setup-python@v7
        with:
          python-version: "3.12"
          cache: pip
          cache-dependency-path: backend/requirements-dev.txt

      - name: Install application, tooling, and browser dependencies
        run: yarn setup --with-deps

      - name: Lint, format, test, and build
        run: yarn lint && yarn format:check && yarn test:unit && yarn test:api && yarn build

      - name: Test Chrome, Safari, and mobile Safari engines
        if: matrix.node == '24.19.0'
        run: yarn test:e2e

      - name: Measure JavaScript coverage
        if: matrix.node == '24.19.0'
        run: yarn test:coverage

      - name: Verify PostgreSQL round trips and backup restore
        run: yarn test:integration
        env:
          TEST_DATABASE_URL: postgres://postgres:postgres@127.0.0.1:5432/postgres
          TEST_BACKUP_CONTAINER: ${{ job.services.postgres.id }}

      - name: Verify web container build
        if: matrix.node == '24.19.0'
        run: docker build --file backend/deploy/raspberry-pi/web/Dockerfile --tag done-ish-web:ci .

      - name: Verify deployed API image and health/auth boundaries
        if: matrix.node == '24.19.0'
        run: |
          docker build --tag done-ish-api:ci backend/api
          node backend/api/test/container-smoke.mjs
        env:
          TEST_DATABASE_URL: postgres://postgres:postgres@127.0.0.1:5432/postgres

      - name: Save JavaScript coverage
        if: always() && matrix.node == '24.19.0'
        uses: actions/upload-artifact@v7
        with:
          name: javascript-coverage
          path: coverage/lcov.info
          if-no-files-found: ignore

      - name: Save browser failure traces
        if: failure()
        uses: actions/upload-artifact@v7
        with:
          name: browser-failure-traces-node-${{ matrix.node }}
          path: test-results/
          if-no-files-found: ignore
          retention-days: 7
